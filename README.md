# FAGIS Console OS

![Banner](https://via.placeholder.com/800x200?text=FAGIS+Console+OS+-+Python+Based+Terminal+Environment)

Локальная консольная операционная система на Python с реализацией базовых команд и интерактивными функциями. Проект создан для демонстрации возможностей создания простых консольных интерфейсов.

## ✨ Основные функции

- 📁 Файловая система: команды `ls`, `cd`, `mkdir`, `touch`
- 📟 Системные утилиты: вывод времени/даты (`time`, `date`), очистка экрана (`clear`)
- 🧮 Встроенный калькулятор (`calc`)
- 🎮 Мини-игра "Угадай число" (`game`)
- 🔍 Система проверки обновлений через GitHub
- 📋 История команд (на базе стандартного ввода Python)
- ⚙️ Гибкое конфигурирование через код

## ⚙️ Установка и запуск

1. **Требования**:
   - Python 3.8+
   - Зависимости: `pip install requests`

2. **Запуск**:
   ```bash
   # Клонировать репозиторий (опционально)
   git clone https://github.com/yourusername/fagis-console.git
   cd fagis-console

   # Запустить систему
   python fagis_console.py
   ```

## 🖥️ Доступные команды

| Команда     | Описание                       | Пример использования       |
|-------------|--------------------------------|----------------------------|
| `help`      | Показать справку               | `help cd`                  |
| `exit`      | Выйти из системы               | `exit`                     |
| `clear`     | Очистить терминал              | `clear`                    |
| `time`      | Показать текущее время         | `time`                     |
| `date`      | Показать текущую дату          | `date`                     |
| `whoami`    | Показать текущего пользователя | `whoami`                   |
| `ls`        | Показать файлы в директории    | `ls`                       |
| `cd`        | Сменить директорию             | `cd Documents`             |
| `mkdir`     | Создать директорию             | `mkdir new_folder`         |
| `touch`     | Создать файл                   | `touch file.txt`           |
| `echo`      | Вывести текст                  | `echo Hello World!`        |
| `calc`      | Открыть калькулятор            | `calc`                     |
| `game`      | Запустить мини-игру            | `game`                     |

## 🛠️ Настройка обновлений

Для проверки обновлений система использует GitHub. Чтобы настроить свой репозиторий:

1. Замените ссылки в конструкторе класса:
```python
def __init__(self):
    ...
    self.update_url = "ВАШ_RAW_URL_К_version.txt"
    self.github_url = "ВАША_ССЫЛКА_НА_РЕЛИЗ"
```

2. Создайте файл `version.txt` в вашем репозитории с номером версии (например: `1.2`)

## 📜 Лицензия

Этот проект распространяется под лицензией **MIT**. Для деталей смотрите файл [LICENSE](LICENSE).

> **Разрешено**:  
> - Коммерческое использование  
> - Модификация  
> - Распространение  
> - Частное использование  

> **При условиях**:  
> - Сохранения копирайта и лицензии  
> - Отказ от ответственности автора  

## 💡 Особенности реализации

- Реальный доступ к файловой системе компьютера
- Кросс-платформенная работа (Windows/Linux/macOS)
- Консольное ASCII-меню
- Защита от ошибок ввода
- Логирование основных операций
- Расширяемая архитектура

![Demo](https://via.placeholder.com/600x300?text=Console+Operation+Demo)  
*Пример работы системы (запустить скриншот на будущее)*
